[build]
  command = "pnpm rebuild sharp && gatsby build"
  publish = "public"

[build.environment]
  NODE_VERSION = "18"
  NPM_FLAGS = "--legacy-peer-deps"

[[build.processing]]
  skip_processing = false
